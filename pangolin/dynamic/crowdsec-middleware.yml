# CrowdSec Traefik bouncer middleware
# security-headers, security-headers-keycloak, and rate-limit are defined in sentinel-middlewares.yml
http:
  middlewares:
    crowdsec-bouncer:
      forwardAuth:
        address: "http://crowdsec-bouncer:8080/api/v1/forwardAuth"
        trustForwardHeader: true
